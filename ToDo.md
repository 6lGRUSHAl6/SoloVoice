# 📋 ToDo: Рефакторинг SoloVoice Discord Bot

## 🎯 Цель
Исправить ошибки в `/info` команде и сделать бот модульным через Cogs структуру.

---

## 🐛 Основные проблемы
- [ ] `/info` показывает "бот не в канале" когда он там находится
- [ ] `/info` не показывает ID канала, время подключения, пользователя
- [ ] Бот перезаходит в войс каждые 15-180 секунд
- [ ] Весь код в одном файле main.py (нужна модульность)

---

## 📂 Фаза 1: Создание структуры проекта ✅ ЗАВЕРШЕНА

### Папки
- [x] Создать `cogs/` папку
- [x] Создать `utils/` папку

### Файлы
- [x] Создать `cogs/__init__.py`
- [x] Создать `cogs/voice_commands.py`
- [x] Создать `utils/__init__.py`
- [x] Создать `utils/voice_session.py`
- [x] Создать `utils/formatting.py`
- [x] Создать `utils/checks.py`

---

## 🔧 Фаза 2: Исправление логики отслеживания сессий ✅ ЗАВЕРШЕНА

### utils/voice_session.py
- [x] Создать класс `VoiceSession` с полями:
  - [x] `channel_id: int`
  - [x] `joined_at: datetime`
  - [x] `added_by_mention: str`
- [x] Добавить метод `get_duration() -> timedelta`
- [x] Добавить метод `is_valid(voice_client) -> bool`

### utils/formatting.py
- [x] Переместить функцию `format_uptime(duration) -> str`

### utils/checks.py
- [x] Переместить функцию `require_admin(interaction) -> bool`

### main.py → on_voice_state_update
- [x] Добавить логирование при срабатывании (в Cog)
- [x] Убедиться что удаляется сессия только при `after.channel is None` (в Cog)
- [x] Проверить что нет мультисрабатывания (в Cog)

---

## 🎯 Фаза 3: Создание Cogs структуры ✅ ЗАВЕРШЕНА

### cogs/voice_commands.py
- [x] Создать класс `VoiceCommandsCog(commands.Cog)`
- [x] Переместить команду `/join`:
  - [x] Логика подключения
  - [x] Создание session
  - [x] Обработка ошибок
- [x] Переместить команду `/leave`:
  - [x] Логика отключения
  - [x] Удаление session
  - [x] Обработка ошибок
- [x] **Переписать команду `/info`** (КРИТИЧНО):
  - [x] `guild.voice_client` как источник истины
  - [x] Если bot в канале → показать ID, время, пользователя
  - [x] Если session is None но voice_client не None → показать базовую инфо
  - [x] Если bot не в канале → показать это
- [x] Переместить `on_voice_state_update` event

### main.py
- [x] Оставить инициализацию `intents` и `bot`
- [x] Оставить `bot_started_at`
- [x] Оставить `voice_sessions = {}`
- [x] Добавить `async def setup_hook()` для загрузки cogs
- [x] Оставить `on_ready()` event
- [x] Оставить `bot.run(TOKEN)`
- [x] Удалить все команды и event handlers (они теперь в cogs)
- [x] Убедиться что `voice_sessions` доступен для cogs

---

## ✅ Фаза 4: Проверка и отладка

### Функциональные тесты
- [ ] `/join <channel_id>` → bot зашел в канал
- [ ] `/info` → все поля заполнены (ID, время, пользователь)
- [ ] Перезагрузить скрипт → `/info` работает
- [ ] `/leave` → bot вышел из канала
- [ ] `/join` от другого пользователя → имя обновилось

### Проверка на reconnect bug
- [ ] Запустить `/join` и наблюдать логи минимум 5 минут
- [ ] Убедиться что нет лишних disconnect/reconnect
- [ ] Проверить что `on_voice_state_update` не срабатывает многократно

### Структура проекта
- [ ] Все cogs загружаются без ошибок
- [ ] Все команды доступны после синхронизации
- [ ] Нет import ошибок
- [ ] Логирование работает

---

## 📊 Прогресс

- **Фаза 1** (Структура): ✅ 10/10
- **Фаза 2** (Исправления): ✅ 8/8
- **Фаза 3** (Cogs): ✅ 12/12
- **Фаза 4** (Проверка): ⏳ 0/7 (следующее)

---

## 📝 Заметки

- VoiceSession хранит информацию о текущем подключении бота
- `guild.voice_client` — реальное состояние, `voice_sessions` — кэш
- Все команды должны быть в одном Cog для группировки
- После создания структуры просто мигрируем код из main.py